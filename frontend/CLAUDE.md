# Journiv React frontend

Follow the canonical sibling instructions in [`AGENTS.md`](AGENTS.md): read
`docs/README.md`, load only the documents it routes to for the task, and do not
recursively read unrelated frontend contracts. This file intentionally contains
no duplicate frontend contract.
